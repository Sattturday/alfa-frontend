import React from "react";
import styles from "./AddTaskButton.module.scss";

function AddTaskButton() {
  return <button type="button" className={styles.button} disabled>
  </button>;
}

export default AddTaskButton;
